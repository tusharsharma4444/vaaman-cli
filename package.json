{
  "name": "vaaman-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/deep-intel && npm run build -w packages/trust-engine && npm run build -w apps/api && npm run build -w apps/cli && npm run build -w apps/dashboard",
    "build:no-dashboard": "npm run build -w packages/core && npm run build -w packages/deep-intel && npm run build -w packages/trust-engine && npm run build -w apps/api && npm run build -w apps/cli",
    "dev": "npm run dev -w apps/api & npm run dev -w apps/dashboard",
    "dev:api": "npm run dev -w apps/api",
    "dev:dashboard": "npm run dev -w apps/dashboard",
    "dev:cli": "npm run dev -w apps/cli",
    "clean": "rimraf apps/*/dist packages/*/dist",
    "pre-scan": "npm run pre-scan -w apps/cli",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
